import React from 'react';
import { createPortal } from "react-dom";

const Toast = () => {
  const check;
  return createPortal(
    <div>
      sdfsadf
    </div>,
    document.body
  );
};

export default Toast;
